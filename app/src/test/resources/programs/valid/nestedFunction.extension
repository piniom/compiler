foo main() -> Int = {
    var mut ext: Int = 5;
    foo nested(a: Int) -> Int = {
        if ext > 3 then {
            a + ext
        } else {
            a - ext
        }
    }
    ext = 2;
    nested(3)
}
