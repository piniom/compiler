foo main() -> Int {
   // Since function definition is a statement clearly it is also an expression
   // It is however as of now unspecified what the value of such expression is so the program could finish with the value of 1 or 2
   if foo f() -> Bool = true then 1 else 2
}