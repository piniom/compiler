
// That is a single line comment
// Everything after "//" will be treated as comment  

// Comment could start as the beginning of a line
foo main() -> Int = { // or starts after working code
    let mut result: Int = 0;
    // result = 1;
    result // program should return 0
}

// Program could ends with comments